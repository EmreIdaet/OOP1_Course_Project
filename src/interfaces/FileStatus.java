package interfaces;

public interface FileStatus {
    /**
     * This interface is used to set the current file path.
     * It provides a method to set the file path for the library system.
     *
     * @param filePath The path of the file to be set as the current file.
     */
    void setCurrentFile(String filePath);
}
